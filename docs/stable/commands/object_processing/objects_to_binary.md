---
layout: docu
redirect_from:
- /docs/commands/objects-to-binary
title: Objects to binary
---

<a href="{{ site.baseurl }}/images/commands/object-to-binary-screenshot.png" data-lightbox="image"><img src="{{ site.baseurl }}/images/commands/object-to-binary-screenshot.png" style="width: 30%" alt="Loading ..."/></a>

The object to binary command uses the objects of the `First operand` applies the selected function / combination with the objects from the `Second operand` and creates a binary image of the result.

This command is useful if once detected objects should be further processed with image processing commands.